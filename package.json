{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "lint": "npm run lint --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "build": "npm run build --prefix frontend",
    "audit": "npm audit && npm audit --prefix backend && npm audit --prefix frontend",
    "verify": "npm run lint && npm test && npm run build && npm run audit",
    "db:generate": "npm run db:generate --prefix backend",
    "db:migrate:deploy": "npm run db:migrate:deploy --prefix backend",
    "deploy:prepare": "npm run deploy:prepare --prefix backend && npm run build"
  },
  "dependencies": {
    "playwright": "^1.58.2",
    "date-fns": "^2.30.0",
    "lodash": "^4.18.1",
    "spawn-command": "^0.0.2",
    "@babel/runtime": "^7.29.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
